const info = require('../info.json');

module.exports = {
    'name' : 'help',
    'aliases' : [],
    'description' : 'Help command',
    execute(message) {
        let payload = message.content.split(" ");
    }
}